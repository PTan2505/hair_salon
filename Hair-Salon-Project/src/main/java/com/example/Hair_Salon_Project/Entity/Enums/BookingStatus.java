package com.example.Hair_Salon_Project.Entity.Enums;

public enum BookingStatus {
    PENDING, // Initial status when a booking is created
    CONFIRMED, // When the booking is accepted by staff
    CANCELLED, // When the booking is cancelled
    COMPLETED // When the service is completed
}
